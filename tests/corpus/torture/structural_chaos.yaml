apiVersion: v1
kind: Service
metadata:
  name: structural-anarchy
  # Duplicate key
  name: duplicate-name
spec:
  # Missing value
  selector:
  ports:
  - protocol: TCP
    port: 80
    # Missing colon
    targetPort 9376
  # Array item without dash properly aligned
   - protocol: UDP
     port: 53
