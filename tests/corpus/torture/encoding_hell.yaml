apiVersion: v1
kind: ConfigMap
metadata:
  name: encoding-hell
  annotations:
    # Emoji key
    💡hint: "Use bright ideas"
    # Zero width space in key
    hidden​key: "gotcha"
data:
  # Binary-ish garbage
  trash: ""
  # Invalid UTF-8 sequence (simulated as raw bytes if possible, but python writes utf-8)
  # We'll rely on weird unicode chars that might trip up ascii decoders
  weird_chars: "ȧƈƈentş and ēmojis 🚀"
  # Massive key
  k{"e"*1000}y: "value"
